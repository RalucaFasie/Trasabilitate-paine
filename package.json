{
  "name": "trasabilitate-paine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint src/**/*.js public/**/*.html",
    "format": "prettier --write \"src/**/*.{js,css}\" \"public/**/*.html\"",
    "dev:api": "json-server --watch frontend/db.json --port 3000"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "json-server": "^0.17.3",
    "prettier": "^3.2.5",
    "vite": "^5.2.0"
  },
  "dependencies": {
    "express-rate-limit": "^8.2.1"
  }
}
