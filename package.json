{
  "name": "trasabilitate-paine",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev:api": "json-server --watch frontend/db.json --port 3000",
    "start:static": "python -m http.server 8000"
  },
  "devDependencies": {
    "json-server": "^0.17.3"
  },
  "dependencies": {
    "express-rate-limit": "^8.2.1"
  }
}