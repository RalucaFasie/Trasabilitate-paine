# Copy to .env and fill
PRIVATE_KEY= # optionally for deploy & local wallet (Hardhat/testnet) - NEVER commit real private keys
MUMBAI_RPC=https://rpc-mumbai.maticvigil.com
SEPOLIA_RPC=https://sepolia.infura.io/v3/YOUR_INFURA_KEY
RPC_URL=http://localhost:8545  # for relayer when running local hardhat node
RELAYER_PK= # relayer private key (for real sending in testnet) - keep secret
CONTRACT_ADDRESS= # after deploy, set this for relayer config